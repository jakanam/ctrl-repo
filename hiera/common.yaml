---
users_common:
  {root}:
    ensure: present
    uid: 0
    groups:
      - root
      #- sudo
    comment: {Root user from puppet}
    managehome: true
    #password: '{password hash}'
    shell:  /bin/bash
    #ssh_authorized_keys:
     # userkey:
     #   type: 'ssh-rsa'
     #   key:  '{public key hash}'
